#!/bin/bash

cd .. && cd ./planting_season_auth_app     && git checkout master  &&  git pull origin master
cd .. && cd ./planting_season_API          && git checkout master  &&  git pull origin master
cd .. && cd ./planting_season_landing_page && git checkout master  &&  git pull origin master
cd .. && cd ./planting_season_coordinator  && git checkout master  &&  git pull origin master
